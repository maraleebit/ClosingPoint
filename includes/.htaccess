# Interdit tout accès HTTP direct : ces fichiers ne doivent être inclus que par PHP (require)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
